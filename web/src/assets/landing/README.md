# Landing assets (Figma → local)

- `unionmeta-logo-horizontal.svg` — UnionMeta wordmark from Figma node `66:4865` (do not hand-edit; re-export from Figma to update).
- Served at runtime via `web/public/landing/` (copy of this file).

Hero background glows use CSS (`.landing-glow` in `landing-theme.css`), not SVG exports.

/*
Copyright (C) 2023-2026 QuantumNous

Landing brand aliases — defaults live in `@/lib/constants` (console Site settings override).
*/
import { DEFAULT_LOGO, DEFAULT_SYSTEM_NAME } from '@/lib/constants'

/** Horizontal wordmark for marketing surfaces (Figma Home / contact dialog). */
export const LANDING_LOGO_SRC = '/landing/unionmeta-logo-horizontal.svg'

/** Light-mode horizontal wordmark (dark glyph on white). */
export const LANDING_LOGO_LIGHT_SRC =
  '/landing/unionmeta-logo-horizontal-light.svg'

/** Square mark used as the site-wide default logo (`DEFAULT_LOGO`). */
export const LANDING_LOGO_MARK_SRC = DEFAULT_LOGO

export const LANDING_BRAND_NAME = DEFAULT_SYSTEM_NAME

